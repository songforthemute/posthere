import React from "react";
import AuthForm from "../components/AuthForm";

const Auth = () => {
    return (
        <div className="authContainer">
            <AuthForm />
        </div>
    );
};

export default Auth;
